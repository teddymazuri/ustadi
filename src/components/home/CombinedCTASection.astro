---
// CombinedCTASection.astro — Partner + Donate in one section
export interface Props {
  heading: string;
  body: string;
  primaryCta: {
    label: string;
    href: string;
  };
  secondaryCta: {
    label: string;
    href: string;
  };
}

const { heading, body, primaryCta, secondaryCta } = Astro.props;
---

<section class="section-padding bg-primary relative overflow-hidden" aria-labelledby="combined-cta-heading">
  <div class="absolute -top-10 -right-10 w-40 h-40 rounded-full bg-white/5" aria-hidden="true"></div>
  <div class="absolute -bottom-8 -left-8 w-32 h-32 rounded-full bg-accent/10" aria-hidden="true"></div>
  
  <div class="container-tight text-center relative">
    <div class="w-16 h-16 mx-auto flex items-center justify-center rounded-2xl bg-accent text-dark mb-6 shadow-lg">
      <svg class="w-8 h-8" fill="none" stroke="currentColor" viewBox="0 0 24 24" aria-hidden="true">
        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M17 20h5v-2a3 3 0 00-5.356-1.857M17 20H7m10 0v-2c0-.656-.126-1.283-.356-1.857M7 20H2v-2a3 3 0 015.356-1.857M7 20v-2c0-.656.126-1.283.356-1.857m0 0a5.002 5.002 0 019.288 0M15 7a3 3 0 11-6 0 3 3 0 016 0z" />
      </svg>
    </div>
    <h2
      id="combined-cta-heading"
      class="font-heading font-bold text-white"
      style="font-size: var(--text-heading-md); line-height: var(--text-heading-md--line-height);"
    >
      {heading}
    </h2>
    <p
      class="mt-5 text-white/70 max-w-lg mx-auto"
      style="font-size: var(--text-body-lg); line-height: var(--text-body-lg--line-height);"
    >
      {body}
    </p>
    <div class="mt-8 flex flex-col sm:flex-row gap-4 justify-center">
      <a
        href={primaryCta.href}
        class="inline-flex items-center justify-center px-8 py-4 font-semibold text-dark bg-accent rounded-xl hover:bg-accent/90 no-underline shadow-lg"
        style="font-size: var(--text-body-base);"
      >
        {primaryCta.label}
        <svg class="w-5 h-5 ml-2" fill="none" stroke="currentColor" viewBox="0 0 24 24" aria-hidden="true">
          <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M17 8l4 4m0 0l-4 4m4-4H3" />
        </svg>
      </a>
      <a
        href={secondaryCta.href}
        class="inline-flex items-center justify-center px-8 py-4 font-semibold text-white border-2 border-white/30 rounded-xl hover:bg-white/10 no-underline"
        style="font-size: var(--text-body-base);"
      >
        {secondaryCta.label}
      </a>
    </div>
  </div>
</section>